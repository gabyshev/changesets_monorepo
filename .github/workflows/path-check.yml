name: path check

on:
  push:
    paths:
      - "apps/**/package.json"
    branches:
      - main

jobs:
  path-check:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "it works!"
